# Dockerfile.prod para Backend (Producción)
FROM node:20-alpine

WORKDIR /app

# Copiar dependencias
COPY package*.json ./
RUN npm ci --only=production

# Copiar código
COPY . .

# Exponer puerto
EXPOSE 8080

# Usar usuario no root
USER node

CMD ["node", "index.js"]
